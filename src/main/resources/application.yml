spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/courseSystem
    username: postgres
    password: mishacoding
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
     ddl-auto: update
    database: postgresql
    show-sql: true